version: "3.8"

services:
  airflow-scheduler:
    volumes: ["./data:/opt/airflow/data"]
  airflow-webserver:
    volumes: ["./data:/opt/airflow/data"]
  airflow-worker:
    volumes: ["./data:/opt/airflow/data"]
  airflow-triggerer:
    volumes: ["./data:/opt/airflow/data"]
  airflow-init:
    volumes: ["./data:/opt/airflow/data"]
